<%= form_with(model: @summary, local: true) do |form| %>
  <% if @summary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@summary.errors.count, "error") %> prohibited this @summary from being saved:</h2>

      <ul>
      <% @summary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="summary-create-table">
  <tr class="book-detail">
    <td class="book-image">
      <%= image_tag(@book.image_url)%>
    </td>
    <td class="book-desc">
      <p>
        <span class="book-title">タイトル　</span>
        <span class="book-ex"><%= @book.title%></span>
      </p>
      <p>
        <span class="book-title">著者　　　</span>
        <span class="book-ex"><%= @book.author%></span>
      </p>
      <p>
        <span class="book-title">出版社　　</span>
        <span class="book-ex"><%= @book.publisher%></span>
      </p>
      <p class="create-category">
        <span class="book-title">ジャンル　</span>
        <span>
          <%= form.select :category, Summary.categories_i18n.invert, include_blank: '選択してください' %>
        </span>
      </p>
    </td>
  </tr>
  </table>

  <hr>

  <div class="container  summary-create-content">
    <div>
      <%= form.rich_text_area :content %>
    </div>

    <div class="center">
      <%= form.submit "更新", class:"search-submit" %>
    </div>
  </div>
<% end %>